{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch ir-remocon-analyzer: default",
            "type": "mplab-core-da",
            "request": "launch",
            "project": "ir-remocon-analyzer",
            "configuration": "default",
            "tool": "${command:pickTool}",
            "stopOnEntry": true,
            "preLaunchTask": "ir-remocon-analyzer: default - Full Build"
        }
    ]
}